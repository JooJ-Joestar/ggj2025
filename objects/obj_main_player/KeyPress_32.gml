/// @description Inserir descrição aqui
// Você pode escrever seu código neste editor

//if (!tree_near || cutting_tree || has_wood) exit;
//cut_tree();
if (!is_main_player) exit;

if (time_source_bomb) exit;
place_bomb();